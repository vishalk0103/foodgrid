import React, { useState } from "react";
import MainHome from "../components/MainHome";

const Home = (props) => {

  return (
    <React.Fragment>
      <div className="bg-white">
        <MainHome  />
      </div>
    </React.Fragment>
  );
};

export default Home;
